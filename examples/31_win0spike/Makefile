APP        = win0spk
SRCS       = main.c

# Compact layout: whole program (code/data/stack/trampoline) in WIN2,
# so it survives the WIN0 page swaps performed by the trampoline.
CODE_LOC   = 0x8100
STACK      = 0xBFFF

include ../common.mk
